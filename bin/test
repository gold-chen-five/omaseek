#!/usr/bin/env bash
# Unit tests for the pure logic in src/lib/*.mjs.
#
# These are plain ES modules, so node runs them directly — no shell, no
# compositor, no network. Everything that can be tested this way should be.

set -euo pipefail
cd "$(dirname "${BASH_SOURCE[0]}")/.."
exec node --test test/*.test.mjs
